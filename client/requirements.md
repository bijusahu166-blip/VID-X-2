## Packages
react-intersection-observer | For infinite scroll detection in the feed
framer-motion | For smooth page transitions and micro-interactions
date-fns | For formatting timestamps (e.g., "2 hours ago")

## Notes
- Using Replit Auth for authentication (/api/login, /api/logout, /api/auth/user)
- Backend provides SSE streaming for AI chat at /api/conversations/:id/messages
- Images are handled via URL strings for now (Post model has imageUrl)
- Design follows a mobile-first "Instagram-like" aesthetic with bottom navigation
